model Course {
  id       Int    @id @default(autoincrement())
  title     String @unique
  description String?
  topics      String[]
  sessions    String[]
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
}